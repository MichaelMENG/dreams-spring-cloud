# TODO: 1. 打通订单出现问题，错误提示：com.netflix.client.ClientException: Load balancer does not have available server for client
# TODO: 2. 解决方案如下，网上找的，http://www.yayihouse.com/yayishuwu/chapter/1288
# TODO: 3. 当前项目Spring Boot -> 1.57，不知道 2.x 会不会有这个问题？
spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
    stream:
      bindings:
        input:
          destination: raw-sensor-data
        output:
          destination: raw-sensor-data

# zipkin 配置
# todo: 1) probability -> 1.0 default 0.1 : 默认发送10%， 测试目的可以100%
#  zipkin:
#    baseUrl: http://192.168.1.99:9411
#    sender:
#      type: web
#  sleuth:
#    sampler:
#      probability: 1.0

eureka:
  client:
    service-url:
      defaultZone: http://eureka-1:8761/eureka/, http://eureka-2:8762/eureka/
  instance:
    prefer-ip-address: true

# hystrix 熔断器配置
hystrix:
  command:
# 全局配置 default
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 1000
# 单独为 getProductInfoList 配置
    getProductInfoList:
      execution:
              isolation:
                thread:
                  timeoutInMilliseconds: 3000

# 配置 feign-hystrix
feign:
  hystrix:
    enabled: true

# 配置 hystrix dashboard
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream, info, health

# sleuth 临时调整logging -> debug
#logging:
#  level:
#    org.springframework.cloud.openfeign: debug

