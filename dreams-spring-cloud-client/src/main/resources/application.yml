eureka:
  client:
    service-url:
      # 高可用，多个Eureka Server, 全部注册上
      # defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: clientName
spring:
  application:
    name: client